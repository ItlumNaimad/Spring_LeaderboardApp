/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    // Wskazuje na wszystkie pliki .html w folderze templates
    "../resources/templates/**/*.html",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
/**
 * Konfiguracja Tailwind CSS.
 * Określa:
 * - ścieżki do plików zawierających klasy Tailwind
 * - customowe rozszerzenia motywu
 * - dodatkowe wtyczki
 * @type {import('tailwindcss').Config}
 */

